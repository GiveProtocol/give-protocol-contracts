version = 1

[[analyzers]]
name = "javascript"
enabled = true

  [analyzers.meta]
  environment = ["nodejs"]

# Slither is disabled here because it times out during dependency installation.
# Slither analysis runs via GitHub Actions workflow (.github/workflows/slither.yml) instead.
